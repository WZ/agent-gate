# goreleaser config — see https://goreleaser.com
# Pure-Go build (CGO_ENABLED=0). One archive per GOOS/GOARCH.
# Triggered by tag push via .github/workflows/release.yml.
version: 2

project_name: agent-gate

before:
  hooks:
    - go mod tidy

builds:
  - id: agent-gate
    main: ./cmd/agent-gate
    binary: agent-gate
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      # Windows ARM64 is rare in agent-gate's audience; re-enable when asked.
      - goos: windows
        goarch: arm64

archives:
  - id: default
    formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE*
      - README.md
      - CLAUDE.md
      - TODOS.md

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: '{{ incpatch .Version }}-next'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^merge'
      - 'Merge pull request'
      - 'Merge branch'

release:
  github:
    owner: WZ
    name: agent-gate
  draft: false
  # Tags follow vX.Y.Z-<slug>; the slug is a semver prerelease identifier,
  # so `auto` correctly marks them as prerelease in the GitHub UI.
  prerelease: auto
  name_template: 'agent-gate {{ .Tag }}'
  header: |
    ## agent-gate {{ .Tag }}

    Single-user audit gate for AI agent HTTPS traffic. See `CLAUDE.md`
    for what this is and `README.md` for getting started.

    ### Install

    Download the archive for your platform below, extract, and put
    `agent-gate` on your `PATH`. Then:

    ```
    agent-gate init
    ```

    On macOS, you may need to clear the quarantine attribute the first
    time:

    ```
    xattr -d com.apple.quarantine ./agent-gate
    ```

    Or install from source:

    ```
    go install agent-gate/cmd/agent-gate@{{ .Tag }}
    ```
  footer: |
    **Full Changelog:** https://github.com/WZ/agent-gate/compare/{{ .PreviousTag }}...{{ .Tag }}
